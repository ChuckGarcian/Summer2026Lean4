## 1

Let $a$ be a set of elements and let $Perm(a)$ be the type of permutations on $a$.


The type of Permtations on $a$ is definitionally equal to congruence between $a$ and $a$ [As stateed here](https://leanprover-community.github.io/mathlib4_docs/Mathlib/Logic/Equiv/Defs.html#Equiv.Perm:~:text=Equations-,Equiv.Perm,),-Instances%20For)

**Claim:**

$$Perm (a) = Perm(Perm(a))$$

Hence,

$$perm (a) = a \cong a$$

We want to show given an instance $p \in Perm(a)$, we can construct an element $\tau \in Perm(Perm(a))$.

Now, any element in $Perm(Perm(a))$ is of the form $Perm(a)\congr Perm(a) $
Observe,

Any,

$$perm (Perm (a)) = a \cong a$$

