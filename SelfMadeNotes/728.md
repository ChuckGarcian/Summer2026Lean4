#check BitVec.Finsupp

/- 2 bit Parity function -/
def parity₂ (fin n →  ): ℝ :=

variable (parity₂ := )

-- example

/-
Output the monimial associated with S
-/

-- def monomialFromS := ∏ x nvariable (n : Nat)

structure BitVecIndicator (n : ℕ) where
  indicator: (Fin n) → BitVec n

-- class approxPolynomial (a: ℕ → α)  where
-- class AproxParity (a: BitVecIndicator n → ℝ) (p : MvPolynomial (BitVecIndicator n) ℝ) where

--   ∀: |p(x) - parity (x) | ≤ (1 / 3)

-- #explode thm2

-- #check (fin n) → BitVec 1

#check (f : (BitVec n) → {q : ℝ // q = 0 ∨ q = 1})

#check BitVecIndicator

#check MvPolynomial.eval f parity2bitPoly

-- ∀ BitVec n  MvPolynomial.eval f circleEquation =

/-
Example 1: Indeed parity₂(x) is exactly represented by polynomial
p(x) = x₁ + x₂ - 2x₁x₂
-/
-- example ∀ MvPolynomial.eval f circleEquation

-- $f : (BitVec n) → {q : ℝ // q = 0 ∨ q = 1}
-- -- #check Polynomial.eval
structure PolyApprox (p : MvPolynomial (σ) ℝ) (f : (BitVec n) → {q : ℝ // q = 0 ∨ q = 1}) where
  -- 1. Indstinguishability on the restricted domain
    indst: ∀ x : → {0,1}, absolute_value (p x - f x) = 0
    -- parity:
      -- ∀ x ∈ Set {0, 1}


-- structure BitVecIndicator (n : ℕ) where
  -- indicator: (Fin n) → BitVec n

-- #check BitVecIndicator

-- --

-- class AproxPoly (a: BitVecIndicator n → ℝ) (p : MvPolynomial (BitVecIndicator n) ℝ) where
--   equal_diff : ∀ n :

-- variable {α : Type u} [AddCommGroup α]

-- variable (p : ) (a : BitVecIndicator n) [inst : AproxPoly (a:= a) ()]

-- namespace PolyRep


-- structure And


-- /- Polynomials representing boolean functions have sum decomposition -/
-- theorem thm1 ()
